#!/usr/bin/env ruby
require_relative './client_terminal'

begin
  ClientTerminal.new.run
rescue Interrupt
  $stdout.puts "interrupt"
  $stdout.flush
rescue SystemCallError => e
  $stderr.puts "#{$0}: #{e.message}"
  exit 1
end
